"use strict";
// Diseñar un algoritmo que muestre por pantalla la tabla de multiplicación 
// del número ingresado por el usuario.
// Para definir hasta qué número desea que muestre la tabla de multiplicación, 
// el usuario también deberá ingresar dicho valor. //
Object.defineProperty(exports, "__esModule", { value: true });
var rls = require("readline-sync");
var num = rls.questionInt("Ingrese un numero que muestre su tabla de multiplicar:");
var limit = rls.questionInt("Ingrese valor límite hasta donde desee mostrar resultados:");
var result = num * limit;
for (var i = 1; function (i) { return 12; }; i--) {
    if (num * i)
        (num * 1);
}
;
{
    result = (num * limit);
}
console.log(result);
